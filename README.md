# 🎨 LaVish Center - نظام إدارة متجر متكامل مع Firebase

## 📋 نظرة عامة

تطبيق متجر LaVish Center مع لوحة تحكم Admin متكاملة ونظام Firebase للتخزين السحابي.

## ✨ الميزات الرئيسية

### 1️⃣ التهيئة الأولية التلقائية
- عند فتح التطبيق لأول مرة، يتم رفع جميع البيانات إلى Firebase تلقائياً:
  - المنتجات (30 منتج)
  - الأقسام (8 أقسام)
  - الفلاتر (4 فلاتر)
  - الإعلانات المتحركة
  - الإعلانات المنبثقة

### 2️⃣ لوحة تحكم Admin
**بيانات تسجيل الدخول:**
- اسم المستخدم: `admin`
- كلمة المرور: `admin`

**الوصول إلى لوحة التحكم:**
1. افتح القائمة الجانبية (☰)
2. اضغط على "تسجيل دخول المدير"
3. أدخل اسم المستخدم وكلمة المرور
4. ستظهر جميع أدوات لوحة التحكم في القائمة

### 3️⃣ إمكانيات لوحة التحكم

#### 📦 إضافة منتج جديد
- اسم المنتج
- القسم
- السعر الحالي
- السعر القديم (اختياري)
- رابط الصورة
- الوصف التفصيلي
- التصنيف (تخفيض/جديد/مميز)
- الفلتر
- التقييم (1-5)
- عدد المراجعات

#### 📁 إضافة قسم جديد
- اسم القسم (بالعربية)
- المعرف (بالإنجليزية)
- أيقونة Font Awesome
- اللون

#### 🔍 إضافة فلتر جديد
- اسم الفلتر
- المعرف
- أيقونة Font Awesome

#### 📢 إضافة إعلان متحرك
- نص الإعلان
- رابط الإعلان
- حالة التفعيل

#### 🎯 إضافة إعلان منبثق
- عنوان الإعلان
- الوصف
- رابط الصورة
- السعر الجديد
- السعر القديم
- الشارة
- معرف المنتج (اختياري)
- حالة التفعيل

## 🔧 التثبيت

### الملفات المطلوبة:
```
├── index.html          (ملف HTML الأصلي)
├── app.js             (ملف JavaScript المحدّث)
├── admin-styles.css   (تنسيقات لوحة التحكم)
├── style.css          (ملف CSS الأصلي)
├── manifest.json
├── sw.js
└── الأيقونات (icon-*.png)
```

### الخطوات:
1. **تحديث ملف index.html:**
   - أضف قبل `</head>`:
   ```html
   <link rel="stylesheet" href="admin-styles.css">
   ```
   
   - أضف قبل `</body>`:
   ```html
   <!-- Firebase SDK -->
   <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-app-compat.js"></script>
   <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-database-compat.js"></script>
   <script src="https://www.gstatic.com/firebasejs/9.22.0/firebase-storage-compat.js"></script>
   
   <!-- Application Script -->
   <script src="app.js"></script>
   ```

2. **استبدل ملف app.js القديم** بالملف الجديد المُنشأ

3. **أضف ملف admin-styles.css** إلى نفس المجلد

4. **ارفع الملفات** إلى السيرفر أو استضافة

## 🎯 كيفية الاستخدام

### للمستخدم العادي:
1. تصفح المنتجات
2. البحث والفلترة
3. إضافة للمفضلة
4. إضافة للسلة
5. عرض تفاصيل المنتج

### للمدير:
1. تسجيل دخول من القائمة الجانبية
2. إضافة منتجات جديدة
3. إنشاء أقسام جديدة
4. إضافة فلاتر
5. إدارة الإعلانات (المتحركة والمنبثقة)

## 🔐 Firebase Configuration

تأكد من صحة إعدادات Firebase في ملف `app.js`:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSyDGpAHia_wEmrhnmYjrPf1n1TrAzwEMiAI",
    authDomain: "messageemeapp.firebaseapp.com",
    databaseURL: "https://messageemeapp-default-rtdb.firebaseio.com",
    projectId: "messageemeapp",
    storageBucket: "messageemeapp.appspot.com",
    messagingSenderId: "255034474844",
    appId: "1:255034474844:web:5e3b7a6bc4b2fb94cc4199",
    measurementId: "G-4QBEWRC583"
};
```

## 📊 هيكل البيانات في Firebase

```
messageemeapp-default-rtdb/
├── initialized (boolean)
├── products/
│   ├── women/
│   ├── men/
│   ├── kids/
│   ├── accessories/
│   ├── shoes/
│   ├── bags/
│   ├── silver/
│   └── gifts/
├── categories/
│   ├── women/
│   ├── men/
│   └── ...
├── filters/
│   ├── all/
│   ├── sale/
│   ├── new/
│   └── popular/
├── scrollingAds/
│   ├── 1/
│   └── 2/
└── popupAds/
    └── 1/
```

## 🎨 تخصيص الألوان

الألوان الأساسية:
- اللون الأساسي: `#ff6b9d`
- اللون الثانوي: `#c44569`
- لون الذهبي: `#ffd700`

## 📱 PWA (Progressive Web App)

التطبيق يدعم:
- تثبيت كتطبيق على الجوال
- العمل بدون إنترنت (Service Worker)
- إشعارات Push (قابلة للتفعيل)

## 🔄 التحديثات المستقبلية

- [ ] نظام الطلبات
- [ ] معالج الدفع
- [ ] نظام التوصيل
- [ ] تقارير المبيعات
- [ ] إدارة المخزون
- [ ] نظام الكوبونات

## 📞 الدعم الفني

للحصول على المساعدة:
- البريد الإلكتروني: info@lavishcenter.com
- WhatsApp: +964 781 379 8636

## 📝 ملاحظات هامة

1. **الأمان:** قم بتغيير بيانات تسجيل دخول Admin قبل النشر
2. **Firebase Rules:** تأكد من تهيئة قواعد الأمان في Firebase
3. **النسخ الاحتياطي:** قم بعمل نسخة احتياطية من البيانات بشكل دوري
4. **الصور:** استخدم روابط HTTPS للصور
5. **الأداء:** راقب استهلاك Firebase Realtime Database

## 🚀 التطوير

للمطورين الذين يريدون تطوير التطبيق:

```javascript
// إضافة منتج برمجياً
async function addProduct(productData) {
    const newProductId = Date.now();
    await database.ref(`products/${productData.category}/${newProductId}`).set({
        id: newProductId,
        ...productData
    });
}

// قراءة جميع المنتجات
database.ref('products').on('value', (snapshot) => {
    const products = snapshot.val();
    console.log(products);
});
```

## 📄 الترخيص

© 2025 LaVish Center - جميع الحقوق محفوظة

---

**صنع بـ ❤️ في العراق**
